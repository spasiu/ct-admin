import paths from '@config/paths';

export default {
  primary: [
    {
      label: 'Home',
      path: paths.home,
    },
    {
      label: 'Products & SKUs',
      path: paths.products,
    },
    {
      label: 'Events & Breaks',
      path: paths.events,
    },
    {
      label: 'Hits',
      path: paths.hits,
    },
  ],
};
